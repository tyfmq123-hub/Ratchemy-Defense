# 범용 프로젝트 개발 워크플로 가이드

## 1. 목적

이 문서는 앞으로 모든 프로젝트에서 공통으로 사용할 개발 운영 기준을 정의한다.

기본 전략은 다음과 같다.

트렁크 기반 GitHub Flow + 워크트리 격리 + 스쿼시 머지 + 작은 PR + 병렬 작업 + 직접 검증

이 방식의 목표는 다음과 같다.

- main 브랜치를 항상 안정적인 기준 상태로 유지한다.
- 작업 단위를 작게 나누어 검토, 병합, 되돌리기를 쉽게 한다.
- git worktree를 사용해 작업 환경을 분리한다.
- 여러 작업자 또는 AI 에이전트가 서로 다른 작업을 병렬로 진행할 수 있게 한다.
- 완료 보고를 그대로 믿지 않고 반드시 직접 검증한다.
- 특정 기술 스택에 의존하지 않는 반복 가능한 개발 운영 방식을 만든다.

이 문서는 앱, 게임, 웹, 서버, 데이터, 문서, 자동화, 연구, 내부 도구 등 모든 프로젝트에 적용할 수 있다.

---

## 2. 이 워크플로의 이름

이 방식은 하나의 공식 명칭으로 고정되어 있지 않다.

기반이 되는 개념은 다음과 같다.

| 구성 요소 | 일반적인 이름 |
|---|---|
| main 중심 개발 | Trunk-Based Development |
| 짧은 기능 브랜치 + PR | GitHub Flow |
| 작은 작업 단위 | Small Pull Request / Single-purpose PR |
| 스쿼시 머지 | Linear History |
| 작업 공간 분리 | Git Worktree Workflow |
| 작업자별 병렬 작업 | Worktree-per-feature / Worktree-per-agent |
| 결과 직접 검증 | Trust but verify |

프로젝트 내부에서는 이 방식을 다음 이름으로 부른다.

트렁크 기반 워크트리 격리 개발 전략

짧게 부를 때는 다음처럼 부른다.

Worktree Flow

---

## 3. 핵심 원칙

### 3.1 main은 항상 안정적인 기준 상태여야 한다

main 브랜치는 언제든 실행, 테스트, 배포, 검토 가능한 기준 상태를 유지한다.

main에 직접 커밋하지 않는다.

모든 변경은 별도 브랜치에서 작업한 뒤 PR을 통해 main에 반영한다.

긴급 수정이 필요해도 hotfix 브랜치를 만들고 PR로 병합한다.

---

### 3.2 작업 단위는 작아야 한다

하나의 작업은 하나의 목적만 가져야 한다.

작업 단위가 작으면 다음 장점이 있다.

- 검토가 쉽다.
- 충돌이 줄어든다.
- 문제가 생겼을 때 원인을 찾기 쉽다.
- PR 단위로 되돌리기 쉽다.
- 병렬 작업에 적합하다.

좋은 작업 단위 예시:

- 로그인 토큰 갱신 오류 수정
- 설정 화면의 테마 선택 UI 추가
- 특정 API 응답 포맷 정리
- 특정 데이터 파일 1세트 검수
- README 설치 절차 보완
- 특정 모듈의 중복 코드 제거

나쁜 작업 단위 예시:

- 전체 코드 정리
- 여러 버그 한 번에 수정
- UI와 서버 로직 동시 변경
- 기능 추가와 대규모 리팩터링 동시 진행
- 포맷팅과 실제 로직 변경 혼합

---

### 3.3 하나의 이슈는 하나의 PR로 처리한다

하나의 이슈는 하나의 명확한 목표를 가져야 한다.

하나의 PR은 하나의 이슈를 해결해야 한다.

여러 목적이 섞이면 다음 문제가 생긴다.

- 리뷰가 어려워진다.
- 테스트 범위가 불명확해진다.
- 일부만 되돌리기 어렵다.
- 병합 충돌 가능성이 커진다.
- 작업자의 책임 범위가 흐려진다.

따라서 큰 작업은 먼저 작은 이슈로 나눈 뒤 각각 PR로 처리한다.

---

### 3.4 브랜치는 짧게 유지한다

브랜치는 오래 유지하지 않는다.

브랜치가 오래 살아 있으면 main과 차이가 커지고 충돌 가능성이 커진다.

권장 방식:

- 작은 작업을 만든다.
- 짧은 브랜치에서 작업한다.
- 빠르게 PR을 만든다.
- 검증 후 main에 병합한다.
- 작업 브랜치와 worktree를 정리한다.

---

### 3.5 squash merge로 main 히스토리를 선형 유지한다

기능 브랜치 안에서는 여러 커밋을 해도 된다.

하지만 main에 병합할 때는 squash merge를 사용한다.

main에는 PR 하나당 커밋 하나가 남도록 한다.

이 방식의 장점:

- main 히스토리가 깔끔하다.
- 변경 이유를 PR 단위로 추적하기 쉽다.
- 문제가 생겼을 때 revert하기 쉽다.
- 배포 노트나 변경 이력 정리가 쉽다.

---

### 3.6 worktree로 작업 환경을 분리한다

하나의 로컬 폴더에서 계속 브랜치를 바꿔가며 작업하지 않는다.

기능별로 별도 worktree를 만든다.

예시 구조:

```text
projects/
  project-main/
  project-fix-login/
  project-add-settings/
  project-refactor-parser/
  project-data-qc-set-01/
```

main worktree는 안정적인 기준점으로 유지한다.

실제 작업은 별도 worktree에서 수행한다.

이렇게 하면 다음 장점이 있다.

- 여러 작업을 동시에 진행할 수 있다.
- IDE나 실행 환경을 덜 흔든다.
- 작업 간 파일 상태가 섞이지 않는다.
- 에이전트나 협업자에게 작업 공간을 분리해서 줄 수 있다.
- 브랜치 전환으로 인한 캐시, 빌드, 설정 꼬임을 줄일 수 있다.

---

### 3.7 완료 보고보다 직접 검증을 우선한다

작업자가 사람이든 AI 에이전트든, “완료했다”는 보고만으로 main에 병합하지 않는다.

항상 다음을 직접 확인한다.

- 변경 파일 목록
- git diff
- 실행 또는 빌드 결과
- 테스트 결과
- 데이터 정합성
- 의도하지 않은 변경 여부
- 되돌리기 가능성

최종 책임자는 작업자가 아니라 프로젝트 소유자다.

---

## 4. 브랜치 전략

### 4.1 기본 브랜치

| 브랜치 | 용도 |
|---|---|
| main | 안정적인 기준 브랜치 |
| feat/* | 기능 추가 |
| fix/* | 버그 수정 |
| refactor/* | 구조 개선 |
| data/* | 데이터 수정 |
| docs/* | 문서 수정 |
| test/* | 테스트 추가 또는 수정 |
| chore/* | 설정, 빌드, 의존성, 기타 작업 |
| hotfix/* | 긴급 수정 |

---

### 4.2 브랜치 이름 규칙

형식:

```text
<type>/<short-purpose>
```

예시:

```text
feat/theme-selector
fix/token-refresh
refactor/payment-module
data/question-set-12-qc
docs/setup-guide
test/add-parser-tests
chore/update-dependencies
hotfix/startup-crash
```

브랜치 이름은 짧고 명확해야 한다.

좋은 브랜치 이름은 작업 목적을 바로 알 수 있어야 한다.

---

## 5. git worktree 기본 사용법

### 5.1 현재 worktree 확인

```bash
git worktree list
```

---

### 5.2 새 작업 worktree 생성

```bash
git fetch origin
git switch main
git pull origin main

git worktree add ../project-fix-token-refresh -b fix/token-refresh main
```

이후 새 worktree로 이동한다.

```bash
cd ../project-fix-token-refresh
```

---

### 5.3 기존 브랜치로 worktree 생성

```bash
git worktree add ../project-existing-work fix/token-refresh
```

---

### 5.4 작업 완료 후 worktree 제거

PR이 main에 병합된 뒤 worktree를 제거한다.

```bash
git worktree remove ../project-fix-token-refresh
```

브랜치도 필요 없으면 삭제한다.

```bash
git branch -d fix/token-refresh
```

원격 브랜치도 삭제할 수 있다.

```bash
git push origin --delete fix/token-refresh
```

---

## 6. 표준 작업 절차

### 6.1 작업 시작

1. 작업을 이슈로 정의한다.
2. 작업 범위를 작게 나눈다.
3. main을 최신 상태로 맞춘다.
4. 작업 브랜치와 worktree를 만든다.
5. 해당 worktree에서만 작업한다.

예시:

```bash
cd project-main
git switch main
git pull origin main

git worktree add ../project-fix-login -b fix/login-error main
cd ../project-fix-login
```

---

### 6.2 작업 중 규칙

작업 중에는 다음을 지킨다.

- 목적 외 파일을 수정하지 않는다.
- 변경 파일 수를 최소화한다.
- 자동 포맷팅으로 대량 변경을 만들지 않는다.
- 기능 추가와 리팩터링을 섞지 않는다.
- 임시 로그, 임시 주석, 실험 코드를 남기지 않는다.
- 불필요한 의존성 추가를 하지 않는다.
- 수정 이유가 불명확한 변경은 하지 않는다.
- 공통 핵심 파일은 동시에 여러 작업에서 수정하지 않는다.

---

### 6.3 작업 완료 후 자체 검증

PR을 만들기 전에 반드시 자체 검증한다.

공통 확인:

```bash
git status
git diff
```

프로젝트에 따라 가능한 검증을 수행한다.

예시:

```bash
npm test
npm run build
npm run lint
```

```bash
pytest
```

```bash
dotnet test
```

```bash
go test ./...
```

```bash
cargo test
```

```bash
./gradlew test
```

검증 명령어가 없는 프로젝트라도 최소한 다음은 확인한다.

- 변경 파일 목록
- 변경 의도와 diff 일치 여부
- 실행 가능 여부
- 핵심 기능 수동 확인
- 데이터 파일 파싱 가능 여부
- 문서 링크 또는 경로 오류 여부

---

### 6.4 커밋

커밋 메시지는 작업 목적이 드러나게 작성한다.

형식:

```text
<type>: <summary>
```

예시:

```text
fix: handle token refresh failure
feat: add theme selector
refactor: simplify payment module
data: correct question set 12
docs: update setup guide
test: add parser edge case tests
chore: update dependencies
```

---

### 6.5 PR 생성

PR 제목은 최종 squash commit 제목이 될 수 있으므로 명확하게 작성한다.

PR 본문에는 다음을 포함한다.

```text
## 목적

## 변경 내용

## 검증 방법

## 영향 범위

## 위험 요소

## 되돌리기 방법
```

---

### 6.6 PR 검토

PR 검토 시 다음을 확인한다.

- 이 PR이 하나의 목적만 가지는가?
- 변경 파일이 의도한 범위 안에 있는가?
- main에 바로 들어가도 안전한가?
- 기존 기능을 망가뜨릴 가능성은 없는가?
- 데이터나 설정 파일이 깨지지 않았는가?
- 되돌리기 쉬운가?
- 검증 결과가 충분한가?

---

### 6.7 머지

문제가 없으면 squash merge로 main에 병합한다.

머지 후 로컬 main을 최신화한다.

```bash
cd project-main
git switch main
git pull origin main
```

그 다음 작업용 worktree를 정리한다.

```bash
git worktree remove ../project-fix-login
```

---

## 7. 병렬 작업 규칙

### 7.1 작업자별 worktree 분리

여러 작업을 동시에 진행할 때는 작업자 또는 작업 단위별로 worktree를 분리한다.

예시:

```text
worker-a → project-fix-login
worker-b → project-add-settings
worker-c → project-data-qc-set-01
```

작업자는 자신의 worktree에서만 작업한다.

main worktree는 직접 수정하지 않는다.

---

### 7.2 작업 배정 시 반드시 정할 것

작업을 배정할 때는 다음을 명확히 한다.

- 작업 목적
- 작업 브랜치
- 작업 worktree
- 수정 허용 파일
- 수정 금지 파일
- 완료 기준
- 검증 방법
- 결과 보고 형식

---

### 7.3 병렬 작업 충돌 방지 규칙

동시에 여러 작업을 진행할 때는 다음을 피한다.

- 같은 파일을 여러 작업자가 동시에 수정
- 공통 설정 파일 동시 수정
- 대규모 포맷팅
- 공통 타입, 스키마, 인터페이스 동시 변경
- 핵심 데이터 구조 동시 변경
- 의존성 파일 동시 변경

충돌 가능성이 높은 파일은 한 번에 한 작업만 수정한다.

---

## 8. AI 에이전트 작업 규칙

### 8.1 에이전트는 보조 작업자다

AI 에이전트는 작업 속도를 높이는 도구다.

최종 판단과 책임은 사람에게 있다.

에이전트가 완료했다고 말해도 반드시 직접 검증한다.

---

### 8.2 에이전트에게 줄 작업 지시 템플릿

```text
작업명:

목표:

작업 브랜치:

작업 worktree:

수정 허용 파일:

수정 금지 파일:

작업 범위:

완료 기준:

검증 방법:

결과 보고 형식:
```

---

### 8.3 에이전트에게 반드시 명시할 규칙

```text
main 브랜치는 수정하지 마세요.
지정된 worktree 안에서만 작업하세요.
요청 범위를 벗어난 리팩터링은 하지 마세요.
불필요한 포맷팅 변경을 만들지 마세요.
의존성 추가가 필요하면 먼저 보고하세요.
공통 파일을 수정해야 하면 먼저 보고하세요.
작업 완료 후 변경 파일 목록과 검증 결과를 보고하세요.
```

---

### 8.4 에이전트 결과 보고 형식

```text
## 작업 요약

## 변경 파일

## 주요 변경 내용

## 검증 결과

## 남은 위험

## 사람이 확인해야 할 부분
```

---

## 9. 검증 기준

### 9.1 공통 검증 기준

모든 PR은 최소한 다음을 확인한다.

- 변경 목적이 명확한가?
- 변경 범위가 작고 제한적인가?
- 의도하지 않은 파일 변경이 없는가?
- 실행 또는 사용 가능한 상태인가?
- 기존 기능을 깨지 않는가?
- 되돌리기 쉬운가?
- 문서 또는 설정 변경이 필요한데 빠지지 않았는가?

---

### 9.2 코드 프로젝트 검증 기준

코드가 포함된 프로젝트에서는 가능한 경우 다음을 확인한다.

- 빌드 성공
- 테스트 통과
- 린트 통과
- 타입 검사 통과
- 핵심 기능 수동 확인
- 로그 또는 예외 확인
- 성능 또는 메모리 영향 확인

---

### 9.3 데이터 프로젝트 검증 기준

데이터가 포함된 프로젝트에서는 다음을 확인한다.

- 파일 파싱 가능 여부
- 스키마 준수 여부
- 필수 필드 누락 여부
- 참조 관계 오류 여부
- 중복 데이터 여부
- 인덱스 또는 ID 충돌 여부
- 실제 사용 화면에서 정상 표시 여부
- 데이터 품질 기준 충족 여부

---

### 9.4 문서 프로젝트 검증 기준

문서가 포함된 프로젝트에서는 다음을 확인한다.

- 내용의 정확성
- 용어 일관성
- 링크 정상 여부
- 경로 정상 여부
- 예제 명령어 실행 가능성
- 독자가 따라 할 수 있는 순서인지 여부

---

### 9.5 설정 및 인프라 프로젝트 검증 기준

설정, 배포, 자동화, 인프라 관련 프로젝트에서는 다음을 확인한다.

- 환경 변수 누락 여부
- 권한 또는 보안 영향
- 배포 영향 범위
- 롤백 가능성
- 로그 노출 위험
- 비용 증가 가능성
- 기존 환경과의 호환성

---

## 10. 금지 사항

다음은 금지한다.

- main 직접 커밋
- 하나의 PR에 여러 목적 섞기
- 대규모 자동 포맷팅 섞기
- 기능 추가와 리팩터링 섞기
- 테스트 또는 수동 확인 없이 머지하기
- 에이전트 결과를 검증 없이 머지하기
- 공통 핵심 파일을 여러 worktree에서 동시에 수정하기
- 의존성 업데이트를 다른 변경과 섞기
- 설정 파일 변경을 설명 없이 반영하기
- 대량 데이터 변경을 검수 없이 반영하기
- 임시 로그, 실험 코드, 디버그 코드를 남긴 채 머지하기

---

## 11. 권장 로컬 디렉터리 구조

로컬에서는 프로젝트별로 다음 구조를 권장한다.

```text
~/Projects/
  project-main/
  project-fix-login/
  project-add-settings/
  project-refactor-core/
  project-data-qc-01/
  project-docs-update/
```

main worktree는 기준 상태로 유지한다.

작업용 worktree는 작업이 끝나면 제거한다.

---

## 12. 이슈 템플릿

```text
## 문제 또는 목표

무엇을 해결하려는지 설명합니다.

## 작업 범위

이번 이슈에서 처리할 범위를 적습니다.

## 제외 범위

이번 이슈에서 하지 않을 일을 적습니다.

## 완료 조건

- [ ] 조건 1
- [ ] 조건 2
- [ ] 조건 3

## 검증 방법

어떻게 확인할지 적습니다.

## 위험 요소

주의해야 할 부분을 적습니다.
```

---

## 13. PR 템플릿

```text
## 목적

이 PR의 목적을 한 문장으로 설명합니다.

## 변경 내용

- 변경 1
- 변경 2
- 변경 3

## 검증 방법

- [ ] 변경 파일 확인
- [ ] 로컬 실행 또는 사용 확인
- [ ] 테스트 또는 수동 검증
- [ ] 영향 범위 확인

## 영향 범위

영향을 받는 기능, 파일, 데이터, 설정, 문서 범위를 적습니다.

## 위험 요소

남아 있는 위험이나 사람이 직접 확인해야 할 부분을 적습니다.

## 되돌리기 방법

문제가 생기면 이 PR을 revert합니다.
```

---

## 14. 에이전트 지시 템플릿

```text
당신은 이 프로젝트의 보조 작업 에이전트입니다.

작업 브랜치:
작업 worktree:

목표:

수정 허용 파일:

수정 금지 파일:

작업 규칙:
- main 브랜치는 수정하지 마세요.
- 지정된 worktree 안에서만 작업하세요.
- 요청 범위를 벗어난 리팩터링은 하지 마세요.
- 불필요한 포맷팅 변경을 만들지 마세요.
- 의존성 추가가 필요하면 먼저 보고하세요.
- 공통 파일 수정이 필요하면 먼저 보고하세요.
- 작업 완료 후 git diff 기준으로 변경 내용을 요약하세요.

완료 기준:

검증 방법:

보고 형식:

## 작업 요약
## 변경 파일
## 검증 결과
## 남은 위험
## 사람이 확인해야 할 부분
```

---

## 15. 체크리스트

### 15.1 작업 시작 체크리스트

- [ ] 이슈가 하나의 목적만 가지는가?
- [ ] 작업 범위가 작게 나뉘었는가?
- [ ] main이 최신 상태인가?
- [ ] 새 브랜치와 worktree를 만들었는가?
- [ ] 수정 허용 파일과 수정 금지 파일을 정했는가?
- [ ] 검증 방법을 정했는가?

---

### 15.2 PR 전 체크리스트

- [ ] git status 확인
- [ ] git diff 확인
- [ ] 의도하지 않은 파일 변경 없음
- [ ] 실행 또는 사용 확인
- [ ] 테스트 또는 수동 검증
- [ ] 데이터/문서/설정 영향 확인
- [ ] PR 본문 작성

---

### 15.3 머지 전 체크리스트

- [ ] PR 목적이 하나인가?
- [ ] 변경 내용이 이해 가능한가?
- [ ] main에 들어가도 안전한가?
- [ ] 되돌리기 쉬운가?
- [ ] 에이전트 또는 작업자 결과를 직접 검증했는가?
- [ ] squash merge 제목이 적절한가?

---

### 15.4 머지 후 체크리스트

- [ ] main pull 완료
- [ ] 작업 worktree 제거
- [ ] 작업 브랜치 삭제
- [ ] 관련 이슈 닫기
- [ ] 필요 시 릴리즈 노트 또는 변경 로그 반영

---

## 16. 운영 판단 기준

이 방식은 다음 상황에 특히 적합하다.

- 혼자 개발하지만 변경 안정성이 중요한 경우
- 여러 작업을 동시에 진행해야 하는 경우
- AI 에이전트와 함께 작업하는 경우
- 실험과 안정 버전을 분리해야 하는 경우
- 데이터, 코드, 문서, 설정 변경이 자주 발생하는 경우
- 되돌리기 쉬운 개발 프로세스가 필요한 경우
- CI가 없거나 약해도 최소한의 안전장치가 필요한 경우

이 방식의 핵심은 빠르게 많이 바꾸는 것이 아니다.

작게 나누고, 격리해서 작업하고, 직접 검증한 뒤, 안전하게 main에 반영하는 것이다.

작게 나눈 작업은 검증하기 쉽다.

검증하기 쉬운 작업은 되돌리기 쉽다.

되돌리기 쉬운 작업은 과감하게 개선할 수 있다.

따라서 앞으로 모든 프로젝트는 이 문서를 기준으로 작업한다.
